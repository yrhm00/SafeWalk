# Backoffice SmartCity
Configure `VITE_API_URL` dans un fichier `.env` si nécessaire (par défaut `http://localhost:3000`).

```
npm run dev
npm install
cd frontend
```bash

## Installation

Backoffice React pour administrer les entités de la base de données (Users, Reports, ReportTypes, Comments, Votes, Zones).


