services:
  db:
    image: postgres:16
    container_name: safewalk-db
    environment:
      POSTGRES_DB: safewalk
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - safewalk-data:/var/lib/postgresql/data

volumes:
  safewalk-data:
