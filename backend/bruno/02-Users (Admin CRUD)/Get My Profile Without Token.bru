meta {
  name: Get My Profile Without Token
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: none
}

tests {
  test("status is 401 (no jwt provided)", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
