meta {
  name: Login Wrong Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: none
}

body:json {
  {
    "email": "florian@mail.com",
    "password": "wrongpassword"
  }
}

tests {
  test("status is 404 (wrong credentials)", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
